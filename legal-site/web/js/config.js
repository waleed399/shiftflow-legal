// Single source of truth for the backend API origin.
// Note: the CSP connect-src in each HTML page lists the same host —
// update those <meta> tags too if this ever changes.
export const API = 'http://localhost:3000/api'
